{% extends 'base.html.twig' %}

{% block title %}Auteurdetails{% endblock %}

{% block body %}
    <h1>Auteurdetails</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>#</th>
                <td>{{ author.id }}</td>
            </tr>
            <tr>
                <th>Naam</th>
                <td>{{ author.name }}</td>
            </tr>
        </tbody>
    </table>

    <div class="mb-4">
        <a href="{{ path('app_author_index') }}">Terug naar overzicht</a> |
        <a href="{{ path('app_author_edit', {'id': author.id}) }}">Bewerken</a>
    </div>

    {{ include('author/_delete_form.html.twig') }}
{% endblock %}
